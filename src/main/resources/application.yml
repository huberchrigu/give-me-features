spring:
  application:
    name: give-me-features
gg:
  jte:
    developmentMode: true # TODO: Fix for production
    templateLocation: src/main/kte
    templateSuffix: .kte
server:
  shutdown: immediate # TODO: Fix for production